
<div class='level'>
  <div class='level-left'>
    <div class='level-item'>
      <p class='title'> <%= @table.name.capitalize %> </p>
    </div>
  </div>

  <div class='level-right'>
    <div class='level-item'>
      <%= link_to 'Inspect schema', table_schema_path(@table.name), class: 'button is-primary' %>
    </div>

    <div class='level-item'>
      <%= render 'railsdb/admin/shared/search', placeholder: "Search in #{@table.name}" %>
    </div>
  </div>
</div>


<div class="table-container">
  <table class="table is-fullwidth is-striped is-hoverable">
    <thead>
      <tr>
        <% @table.columns.each do |column| %>
          <th><%= column.name %></th>
        <% end %>
      </tr>
    </thead>

    <tbody>
      <% @table.data.each do |row| %>
        <tr>
          <% row.each do |key, value| %>
            <td> <%= value %>  </td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

